//Check if one of two numbers is 50 or if their sum is 50

let x = 20;
let y = 30;

let result;

if (x === 50 || y === 50 || (x + y === 50)) {
    result = true;
} else {
    result = false;
}

console.log(result);